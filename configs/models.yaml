teacher:
  name: meta-llama/Llama-3.1-8B
  dtype: float16
  # On Colab A100-40GB you can keep teacher in fp16.
  # On V100/L4, override to load_in_8bit at runtime.

student:
  name: meta-llama/Llama-3.2-1B
  dtype: float16

tokenizer:
  # Same tokenizer family — we use the student's to tokenize inputs.
  name: meta-llama/Llama-3.2-1B
  max_length: 2048
